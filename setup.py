from setuptools import setup, find_packages

setup(
    name='effemb',
    version='0.1.0',
    url='https://github.com/ANON',
    author='ANON ANON, ANON ANON, ANON ANON',
    description='''
    A package for training and evaluating embeddings models based on pre-trained
    open-source LLMs.
    ''',
    packages=find_packages(),
    python_requires=">=3.10",
)
